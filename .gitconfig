[user]
    name = Arthur Alvim
    email = afmalvim@gmail.com
[core]
    editor = subl -w
    autocrlf = input
[color]
    ui = true
[alias]
    logo = log --oneline --decorate --all --graph
    logor = log --reverse --oneline --decorate --all
    s = status -s
    pushall = push --recurse-submodules=on-demand
[init]
    templatedir = ~/.dotfiles/git/init_template
